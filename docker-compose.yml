version: '3.8'

services:
  sms-dashboard:
    build: .
    ports:
      - "5000:5000"
    devices:
      - "/dev/ttyUSB0"
    volumes:
      - data:/app/data
      - templates:/app/templates
    environment:
      - FLASK_ENV=development
      - FLASK_APP=app.py

volumes:
  data:
  templates:
